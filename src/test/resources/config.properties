# Execution Mode (local / remote)
executionMode=local

# Browser Type (chrome / firefox / edge)
browser=chrome

# Selenium Grid URL (only used in remote mode)
gridURL=http://localhost:4444

# Implicit wait time in seconds
implicitWait=10
